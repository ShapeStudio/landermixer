# Required — get one at https://console.anthropic.com
ANTHROPIC_API_KEY=

# Optional — verified LinkedIn profile data via https://nubela.co/proxycurl
# Without it, research relies on public web search only (still works well
# for people with any public footprint).
PROXYCURL_API_KEY=
